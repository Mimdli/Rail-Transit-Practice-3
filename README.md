# Rail-Transit-Practice-3 — 轨道交通模拟系统

软件暑期实训3 —— 基于 Python + Qt 的轨道交通运行模拟系统。

## 项目简介

本系统面向轨道交通运行模拟，目标是在有限实训周期内实现一个**可演示、可交互、可扩展**的轨道交通模拟系统。

系统模拟列车在线路上的基本运行过程，涵盖车辆动力学、线路条件、信号约束、供电状态、车门联锁和运行日志等核心要素，可用于手动/自动驾驶演示、安全逻辑验证及运行评价。

## 技术栈

| 技术 | 用途 |
|------|------|
| **Python 3.8+** | 主开发语言 |
| **PyQt5 / PySide6** | 桌面 GUI 框架，实现可视化界面与交互控制 |
| **openpyxl** | 读取 Excel 格式的线路数据 |
| **PyPDF2** | (可选) 读取 PDF 文档数据 |
| **pytest** | 单元测试 |

## 功能模块

```
┌─────────────────────────────────────────────────────────────┐
│                    轨道交通模拟系统                            │
├──────────┬──────────┬──────────┬──────────┬─────────────────┤
│ 车辆仿真  │ 轨道线路  │ 信号系统  │ 供电状态  │ 可视化与日志      │
├──────────┼──────────┼──────────┼──────────┼─────────────────┤
│ · 速度计算│ · 站点数据│ · 红/黄/ │ · 正常   │ · 状态面板       │
│ · 位置计算│ · 区间距离│   绿灯   │ · 低压   │ · 控制按钮       │
│ · 牵引/制 │ · 限速区段│ · 闭塞占用│ · 断电   │ · 线路位置显示    │
│   动/惰行 │ · 坡度区段│ · 黄灯限速│ · 故障   │ · 日志列表       │
│ · 车门联锁│ · 站台侧 │ · 前方信号│   恢复   │ · 运行评价       │
│ · 运行模式│ · 信号机 │ · 协议灯码│          │ · 统计面板       │
└──────────┴──────────┴──────────┴──────────┴─────────────────┘
```

### 1. 车辆仿真模块

当前界面仍使用旧单质点 `VehicleModel` 驱动一辆可操控列车，体现：
- **牵引/制动/惰行**三种控制状态
- 上坡加速能力下降、下坡制动距离变长
- 速度越高空气阻力越明显
- 支持手动驾驶和自动运行两种模式

同时，后端已合入新的多体车辆动力学框架，供后续 UI 迁移和更真实仿真使用：
- `VehicleController`：多体列车顶层控制器，管理编组状态、控制指令和仿真步进
- `PerCarDynamicsPipeline`：逐车计算牵引、制动、阻力、坡度、车钩力和状态更新
- `TrainConsist` / `CarConfig` / `CarState`：描述列车编组、单车参数和单车状态
- `AutoDriveController`：面向新控制器的自动驾驶控制逻辑
- `ForceReport`：输出每步动力学分解结果，便于调试和评价

注意：当前 Qt 前端尚未迁移到新 `VehicleController`，因此界面暂时不会展示每节车状态、车钩力或多车编组细节。

当前前端车辆计算逻辑（旧 `VehicleModel`）：
1. 主界面每 `100 ms` 刷新一次仿真步。
2. 根据列车当前位置查询线路坡度和线路限速。
3. 根据信号系统计算有效限速：线路限速会受到黄灯限速约束。
4. 根据当前控制级位计算牵引/制动加速度：
   - 低牵引 = `0.3 * max_traction`
   - 中牵引 = `0.6 * max_traction`
   - 满牵引 = `max_traction`
   - 常用制动/全制动/紧急制动分别使用预设制动减速度
5. 计算基本运行阻力：`-(a + b*v + c*v²)`。
6. 计算坡度加速度：`-9.81 * 坡度‰ / 1000`。
7. 合成总加速度：`牵引/制动 + 基本阻力 + 坡度加速度`。
8. 用 `v = v + a * dt` 更新速度，并裁剪到 `[0, 当前限速, 车辆最高速度]`。
9. 用 `position = position + v * dt` 更新位置。

新多体车辆后端计算逻辑（`VehicleController` + `PerCarDynamicsPipeline`）：
1. `VehicleController` 持有列车编组、各节车状态、线路查询接口和环境查询接口。
2. 控制指令以 `throttle` / `brake_level` 输入，并经过一阶低通滤波，模拟牵引建磁和制动响应迟滞。
3. 一个外部步长会被拆成多个物理微步，避免车钩力和高速积分不稳定。
4. 每个微步先查询各节车所在位置的限速、坡度、曲线、隧道和黏着条件。
5. 对每节车分别计算 Davis 阻力、坡度阻力、隧道阻力、曲线阻力、牵引力、电制动力和空气/摩擦制动力。
6. 根据相邻车辆的位置差、速度差和车钩参数计算车钩力。
7. 聚合每节车合力，按等效质量积分更新单车速度、位置和加速度。
8. 对各节车执行限速裁剪，并生成 `ForceReport`，记录牵引、制动、阻力、车钩力和限幅情况。

新自动驾驶逻辑（`AutoDriveController`）：
1. 没有目标或线路接口时保持安全惰行。
2. 根据头车到目标停车点的沿线距离分三段控制：
   - 距离小于紧急制动距离：紧急制动。
   - 距离小于停车控制距离：按距离比例降低目标速度，过快则常用制动，过慢则低牵引。
   - 距离较远：按线路限速的一定比例巡航，低于巡航速度则中牵引，否则惰行。

### 2. 轨道线路模块

从 Excel 线路数据文件中读取线路基础数据，提供：
- 车站、站台位置与方向
- 区间距离计算
- 动态限速与坡度查询
- 信号机位置加载与坐标映射
- 到站站台侧判断

### 3. 信号系统模块

模拟基本信号显示与限速约束：
- **绿灯** — 允许正常运行
- **黄灯** — 允许运行，需降低速度
- **红灯** — 禁止越过，作为防护状态显示与检查
- 根据前车位置生成闭塞分区红/黄/绿状态
- 支持协议灯色码归约为红/黄/绿三态
- 黄灯会参与有效限速计算，取线路限速和黄灯限速的较小值

当前实现以状态生成、显示和限速约束为主；完整 CBTC、联锁进路、红灯自动停车曲线和发车许可尚未实现。

信号灯计算逻辑：
1. 先按运行方向对信号机排序。当前主程序默认按上行方向处理，即信号机位置从小到大排列。
2. 相邻两个信号机之间视为一个简化闭塞分区。
3. 根据前车位置判断其占用的闭塞分区：
   - 前车位于当前信号机防护的下一个闭塞分区时，该信号机显示**红灯**。
   - 前车位于再下一个闭塞分区时，该信号机显示**黄灯**。
   - 前方相关闭塞分区未被占用时，该信号机显示**绿灯**。
4. 未显式配置的信号机状态按红灯防护处理；没有信号机约束的位置默认按绿灯显示。
5. 车辆有效限速 = 线路限速与信号限速共同作用：
   - 前方防护范围内最近信号为黄灯时，限速取 `min(线路限速, 10 m/s)`。
   - 绿灯或无黄灯约束时，保持线路限速。
6. 仪表盘显示为了便于观察，会展示全线最近前方信号；但实际限速计算仍只使用信号系统的前方防护范围，默认 `200 m`。

### 4. 供电状态模块

模拟供电条件对牵引能力的影响：
- 供电正常 / 电压低（限制牵引）/ 断电（禁止牵引）/ 故障恢复

### 5. 可视化展示模块

基于 Qt 的图形界面，实时展示：
- 当前速度、位置、限速、坡度
- 信号状态、供电状态、车门状态
- 当前站/下一站、下一站距离
- 运行日志与安全事件统计

### 6. 日志与运行评价模块

记录运行全过程并输出评价指标：
- 发车/到站事件、停车误差
- 超速事件、红灯违规、紧急制动
- 供电异常记录
- 评价指标：停车误差、最大速度、超速次数、安全事件数

## 项目结构

```
Rail-Transit-Practice-3/
├── resource/                  # 资源文件
│   ├── 线路数据(1).xls       # 原始线路 Excel 数据
│   └── 初步目标需求分析.md   # 需求分析文档
├── src/                       # 源代码
│   ├── main.py               # 程序入口
│   ├── vehicle/              # 车辆仿真模块
│   │   ├── __init__.py
│   │   ├── model.py          # 旧单质点模型（当前 UI 兼容使用）
│   │   ├── controller.py     # 旧手动/自动控制器（当前 UI 兼容使用）
│   │   ├── vehicle_controller.py # 新多体车辆顶层控制器
│   │   ├── dynamics_pipeline.py  # 逐车动力学计算流水线
│   │   ├── forces.py         # 牵引/制动/阻力等力计算
│   │   ├── auto_drive.py     # 新自动驾驶控制器
│   │   ├── coupler.py        # 车钩力计算
│   │   ├── environment.py    # 环境查询接口与默认环境
│   │   └── force_report.py   # 动力学计算报告
│   ├── common/               # 公共数据结构
│   │   ├── car_config.py     # 单车参数与车钩参数
│   │   ├── car_state.py      # 单车状态
│   │   ├── consist.py        # 列车编组
│   │   └── track_position.py # 线路位置与线路查询接口
│   ├── track/                # 轨道线路模块
│   │   ├── __init__.py
│   │   ├── loader.py         # Excel 数据加载器
│   │   └── data.py           # 线路数据结构
│   ├── signal/               # 信号系统模块
│   │   ├── __init__.py
│   │   └── system.py         # 信号状态逻辑
│   ├── power/                # 供电状态模块
│   │   ├── __init__.py
│   │   └── supply.py         # 供电状态管理
│   ├── door/                 # 车门联锁模块
│   │   ├── __init__.py
│   │   └── interlock.py      # 车门与站台联锁
│   ├── logger/               # 日志与评价模块
│   │   ├── __init__.py
│   │   ├── recorder.py       # 事件记录
│   │   └── evaluator.py      # 运行评价
│   └── ui/                   # 可视化界面
│       ├── __init__.py
│       ├── main_window.py    # 主窗口
│       ├── dashboard.py      # 状态面板
│       └── controls.py       # 控制按钮
├── tests/                    # 单元测试
│   ├── test_vehicle.py
│   ├── test_controller.py
│   ├── test_forces.py
│   ├── test_dynamics_pipeline.py
│   ├── test_auto_drive.py
│   ├── test_consist.py
│   ├── test_car_config.py
│   ├── test_car_state.py
│   ├── test_coupler.py
│   ├── test_environment.py
│   ├── test_force_report.py
│   ├── test_track_position.py
│   ├── test_track.py
│   └── test_signal.py
├── data/                     # 运行时数据文件
├── .gitignore
├── README.md
└── requirements.txt          # Python 依赖
```

## 快速开始

### 环境要求

- Python 3.8+
- pip（Python 包管理器）

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行程序

```bash
python src/main.py
```

## 需求优先级

| 优先级 | 内容 |
|--------|------|
| **Must** | 车辆运行、线路站点、限速、坡度、手动控制、状态显示、日志记录 |
| **Should** | 自动停站、信号约束、供电异常、车门联锁 |
| **Could** | 天气影响、载荷模式、黏着控制、防空转/防滑、前端接入多体车辆控制器 |
| **Won't** | 完整 CBTC、完整联锁表、真实供电网络 |

## MVP 定义

> 一列车在真实或简化线路数据上运行，用户可以控制牵引、制动、惰行和车门；系统根据当前位置读取限速、坡度、站点和信号状态，并对超速、红灯、供电异常、车门未关等情况进行约束和日志记录。

## 后续扩展方向

- **天气与黏着条件**：晴天/雨天/冰雪切换，轮轨黏着系数限制
- **载荷与车辆质量**：AW0（空载）/ AW2（定员）/ AW3（超员）模式
- **防空转/防滑**：轮轨保护逻辑
- **多体车辆前端集成**：将 Qt 界面从旧 `VehicleModel` 迁移到 `VehicleController`，展示编组、单车状态和车钩力

## 许可

本项目为软件暑期实训3课程项目。
